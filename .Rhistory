# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[[1]],n=10L))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[[]],n=10L))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[[1:3]],n=10L))
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[[1:2]],n=10L))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[1:3,n=10L))
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[1:3],n=10L))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
print(head(xynames[[1]],n=10L))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
print(head(coords.matrix,n=10L))
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(x = coords.matrix, centers = 7)
print(coords.clusters.7)
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 10,
nstart = 1,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(cl$centers, col = 1:7, pch = 8, cex = 2)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(cl$centers, col = 1:2, pch = 8, cex = 2)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(cl$centers, pch = 8, cex = 2)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
rect(par("usr")[1],par("usr")[3],par("usr")[2],par("usr")[4],col = "gray")
plot(x = coords.matrix, col = coords.clusters.7$cluster)
rect(par("usr")[1],par("usr")[3],par("usr")[2],par("usr")[4],col = "gray")
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
rect(par("usr")[1],par("usr")[3],par("usr")[2],par("usr")[4],col = "gray")
#rect(par("usr")[1],par("usr")[3],par("usr")[2],par("usr")[4],col = "gray")
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 10,
nstart = 1,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
print(coords.clusters.7$cluster)
plot(
x = coords.x,
y = coords. y,
plot(
x = coords.x,
y = coords.y,
color = coords.clusters.7$cluster
)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.y,ncol=2),
matrix(coords.x,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 10,
nstart = 1,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 10,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 10,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
coords.clusters.7 <- kmeans(
x = coords.matrix,
centers = 7,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters.7)
plot(x = coords.matrix, col = coords.clusters.7$cluster)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters.7$cluster
)
points(coords.clusters.7$centers, col = 1:7 , pch = 8, cex = 2)
source(file='../scripts/SUMO.project2plot.R')
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
# Clean up
rm(junctions.matrix,xynames)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
ncenters <- 3
coords.clusters <- kmeans(
x = coords.matrix,
centers = ncenters,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters)
plot(x = coords.matrix, col = coords.clusters$cluster)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters$cluster
)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
# Get a junctions.matrix from the json files for project `leopard`
junctions.matrix <- project.name2jm('leopard')
# Extract the data we want from junction.data at column 'True Coords' (default)
xynames <- jm2xynames(junctions.matrix)
# Summarize our object `xynames`
print(summary(xynames))
# Unlist and give x, y, and names their own vectors.
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
save(xynames, file='xynames.RData')
# Clean up
rm(junctions.matrix,xynames)
xynames <- load('xynames')
xynames <- load('xynames.RData')
load('xynames.RData')
summary(xynames)
rownames(xynames) <- c('x','y','junction')
rownames(xynames) <- c('x','y','bane')
names(xynames) <- c('x','y','name')
summary(xynames)
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
plot(
x=coords.x,
y=coord.y
)
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
plot(
x=coords.x,
y=coords.y
)
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
plot(
x = coords.x,
y = coords.y
)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
ncenters <- 3
coords.clusters <- kmeans(
x = coords.matrix,
centers = ncenters,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters)
ncenters <- 7
coords.clusters <- kmeans(
x = coords.matrix,
centers = ncenters,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
plot.new()
plot(x = coords.matrix, col = coords.clusters$cluster)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
plot(x = coords.matrix, col = coords.clusters$cluster)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
load('xynames.RData')
names(xynames) <- c('x','y','name')
summary(xynames)
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
plot(
x = coords.x,
y = coords.y
)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
ncenters <- 7
coords.clusters <- kmeans(
x = coords.matrix,
centers = ncenters,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters)
plot(x = coords.matrix, col = coords.clusters$cluster)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
plot(
x = coords.x,
y = coords.y,
col = coords.clusters$cluster
)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
load('xynames.RData')
names(xynames) <- c('x','y','name')
summary(xynames)
class(coords.x)
coords.x <- unlist(xynames[1])
coords.y <- unlist(xynames[2])
coords.names <- unlist(xynames[3])
plot(
x = coords.x,
y = coords.y
)
coords.matrix <- rbind(
matrix(coords.x,ncol=2),
matrix(coords.y,ncol=2)
)
colnames(coords.matrix) <- c('x','y')
rownames(coords.matrix) <- coords.names
print(head(coords.matrix,n=10L))
newone <- cbind(coords.x, coords.y)
plot(newone)
ncenters <- 15
coords.clusters <- kmeans(
x = newone,
centers = ncenters,
iter.max = 50,
nstart = 27,
algorithm = 'Hartigan-Wong'
)
print(coords.clusters)
plot(x = newone, col = coords.clusters$cluster)
points(coords.clusters$centers, col = 1:ncenters , pch = 8, cex = 2)
library(tidyverse)
View(junctions.matrix)
View(edges.matrix)
View(edges.list)
